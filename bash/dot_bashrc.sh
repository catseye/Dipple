# Not a fully formed script; intended to be copied into, or sourced from,
# .bashrc or .bash_profile.
# Also woefully incomplete right now.

export EDITOR=nano
export PATH="$HOME/bin:$PATH"

bind '"\e[A":history-search-backward'
bind '"\e[B":history-search-forward'
